{
  "name": "openclaw-aicc-plugin-native",
  "version": "0.1.0",
  "description": "OpenClaw 原生插件：接管模型调用并通过 AICC 机密推理节点转发（含密文链路桩实现）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.1",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "@openclaw/plugin-sdk": ">=0.0.0"
  },
  "peerDependenciesMeta": {
    "@openclaw/plugin-sdk": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
